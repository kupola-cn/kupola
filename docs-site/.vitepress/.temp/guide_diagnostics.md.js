import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"诊断信息","description":"","frontmatter":{},"headers":[],"relativePath":"guide/diagnostics.md","filePath":"guide/diagnostics.md"}');
const _sfc_main = { name: "guide/diagnostics.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="诊断信息" tabindex="-1">诊断信息 <a class="header-anchor" href="#诊断信息" aria-label="Permalink to &quot;诊断信息&quot;">​</a></h1><p>Kupola 的运行时诊断使用稳定 code，方便搜索日志、编写测试和定位文档。</p><p>格式：</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[kupola W001] &lt;button&gt; has an empty @click expression.</span></span>
<span class="line"><span>[kupola E001] Error evaluating k-text on &lt;span&gt;: user.name</span></span></code></pre></div><p>核心运行时只包含 code 和简短消息。详细原因和修复建议放在文档中，避免增加核心库体积。</p><h2 id="errors" tabindex="-1">Errors <a class="header-anchor" href="#errors" aria-label="Permalink to &quot;Errors&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>含义</th><th>推荐修复</th></tr></thead><tbody><tr><td><code>E001</code></td><td>指令表达式执行失败。</td><td>检查表达式中的变量是否存在、对象是否为 <code>null</code>、方法是否抛错。</td></tr><tr><td><code>E002</code></td><td><code>k-for</code> 表达式格式错误。</td><td>使用 <code>item in items</code> 或 <code>(item, index) in items</code>。</td></tr><tr><td><code>E003</code></td><td><code>k-data</code> 解析失败的兜底错误。</td><td>检查 <code>k-data</code> 表达式是否是对象字面量或已注册的 scope 名称。</td></tr></tbody></table><h2 id="warnings" tabindex="-1">Warnings <a class="header-anchor" href="#warnings" aria-label="Permalink to &quot;Warnings&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>含义</th><th>推荐修复</th></tr></thead><tbody><tr><td><code>W001</code></td><td>需要表达式的指令为空。</td><td>补上表达式，或删除该指令。</td></tr><tr><td><code>W002</code></td><td>指令缺少必要参数，例如 <code>k-on</code> 没有事件名。</td><td>使用 <code>@click=&quot;...&quot;</code> 或 <code>k-on:click=&quot;...&quot;</code>。</td></tr><tr><td><code>W003</code></td><td><code>k-model</code> 用在了非表单元素上。</td><td>只在 <code>&lt;input&gt;</code>、<code>&lt;select&gt;</code>、<code>&lt;textarea&gt;</code> 上使用 <code>k-model</code>。</td></tr><tr><td><code>W004</code></td><td>同一次 <code>k-for</code> 渲染中出现重复 key。</td><td>使用数据库 id、稳定业务 id，或生成唯一 key。</td></tr><tr><td><code>W005</code></td><td>同一元素同时使用 <code>k-for</code> 和 <code>k-if</code>。</td><td>用外层包裹拆开列表和条件生命周期。</td></tr><tr><td><code>W006</code></td><td>同一元素同时使用 <code>k-class</code> 和 <code>:class</code> / <code>k-bind:class</code>。</td><td>条件 class 用 <code>k-class</code>，完整替换 class 才用 <code>:class</code>。</td></tr><tr><td><code>W007</code></td><td>同一元素同时使用 <code>k-style</code> 和 <code>:style</code> / <code>k-bind:style</code>。</td><td>条件 style 用 <code>k-style</code>，完整替换 style 才用 <code>:style</code>。</td></tr><tr><td><code>W008</code></td><td>同一元素同时使用 <code>k-model</code> 和 <code>:checked</code> / <code>k-bind:checked</code>。</td><td>让 <code>k-model</code> 独占 checked 状态。</td></tr><tr><td><code>W009</code></td><td>同一元素同时使用 <code>k-model</code> 和 <code>:value</code> / <code>k-bind:value</code>。</td><td>让 <code>k-model</code> 独占 value 状态；checkbox/radio 的动态 value 除外。</td></tr><tr><td><code>W010</code></td><td><code>k-else-if</code> / <code>k-else</code> 没有紧邻 <code>k-if</code>。</td><td>把分支放在同一层级并紧跟 <code>k-if</code> 后面。</td></tr><tr><td><code>W011</code></td><td><code>k-transition</code> 没有配合 <code>k-show</code> 或 <code>k-if</code>。</td><td>加上 <code>k-show</code> / <code>k-if</code>，或删除 <code>k-transition</code>。</td></tr><tr><td><code>W012</code></td><td>同一个 root 被重复 <code>walk()</code>。</td><td>使用 <code>walkOnce()</code>，或先 <code>destroyWalk(root)</code> 再重新初始化。</td></tr><tr><td><code>W013</code></td><td><code>k-data</code> 引用了未注册的命名 scope。</td><td>先调用 <code>defineScope(name, ...)</code>，或改成对象字面量。</td></tr></tbody></table><h2 id="常见示例" tabindex="-1">常见示例 <a class="header-anchor" href="#常见示例" aria-label="Permalink to &quot;常见示例&quot;">​</a></h2><p>空表达式：</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">&lt;!-- 会触发 W001 --&gt;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">button</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> @click</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;保存&lt;/</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">button</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span></code></pre></div><p>修复：</p><div class="language-html vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">html</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">button</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> @click</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;save()&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;保存&lt;/</span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">button</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&gt;</span></span></code></pre></div><p>重复初始化：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 会触发 W012</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">walk</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(root)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">walk</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(root)</span></span></code></pre></div><p>修复：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">walkOnce</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(root)</span></span></code></pre></div><p>或在替换后端片段前销毁旧实例：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">destroyWalk</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(root)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">root.innerHTML </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> await</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> fetch</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;/users/fragment&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">).</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">then</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">r</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> r.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">text</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">())</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">walkOnce</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(root)</span></span></code></pre></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/diagnostics.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const diagnostics = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  diagnostics as default
};
